{
  "main": "pokeymanz.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c",
    "build:css": "sass styles/scss/pokeymanz.scss styles/pokeymanz.css --style=expanded --no-source-map",
    "build:css:watch": "sass styles/scss/pokeymanz.scss styles/pokeymanz.css --style=expanded --source-map --watch"
  },
  "devDependencies": {
    "rollup": "^4.18.0"
  },
  "dependencies": {
    "prettier": "^3.3.3",
    "sass": "^1.79.4"
  }
}
